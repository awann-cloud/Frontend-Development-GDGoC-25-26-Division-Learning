// Destructuring Object
const user = {
  nama: "Raby",
  umur: 21,
};

const { nama, umur } = user;

console.log(nama);
console.log(umur);

// Destructuring Array
const angka = [1, 2, 3, 4, 5];  
const [a, b, c] = angka;

console.log(a);